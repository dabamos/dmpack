.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "DMIMPORT" "1" "" "Version 2.0.0" "User Commands"
.SH NAME
dmimport \(en imports records from file into database
.SH SYNOPSIS
\f[B]dmimport\f[R] \-\-help
.PP
\f[B]dmimport\f[R] \-\-version
.PP
\f[B]dmimport\f[R] \-\-\f[B]input\f[R] \f[I]file\f[R]
\-\-\f[B]database\f[R] \f[I]file\f[R] \-\-\f[B]type\f[R] \f[I]type\f[R]
[\-\-\f[B]quote\f[R] \f[I]char\f[R]] [\-\-\f[B]separator\f[R]
\f[I]char\f[R]] [\-\-\f[B]dry\f[R]] [\-\-\f[B]verbose\f[R]]
.SH DESCRIPTION
The \f[B]dmimport\f[R] program reads logs, nodes, sensors, targets, and
observations in CSV format from file and imports them into the database.
The database inserts are transaction\-based.
If an error occurs, the transaction is rolled back, and no records are
written into the database at all.
.PP
The database has to be a valid DMPACK database and must contain the
tables required for the input records.
The nodes, sensors, and targets referenced by input observations must
exist in the database.
The nodes referenced by input sensors must exist as well.
.SH OPTIONS
.TP
\f[B]\-\-database\f[R], \f[B]\-d\f[R] \f[I]file\f[R]
Path of the SQLite database (required, unless in dry mode).
.TP
\f[B]\-\-dry\f[R], \f[B]\-D\f[R]
Enable dry mode.
Read records from file and validate them without actual database import.
.TP
\f[B]\-\-help\f[R], \f[B]\-h\f[R]
Print available command\-line arguments and quit.
.TP
\f[B]\-\-input\f[R], \f[B]\-i\f[R] \f[I]file\f[R]
Path of input file in CSV format.
.TP
\f[B]\-\-quote\f[R], \f[B]\-q\f[R] \f[I]char\f[R]
CSV quote character.
If not passed, quoting is disabled by default.
.TP
\f[B]\-\-separator\f[R], \f[B]\-s\f[R] \f[I]char\f[R]
CSV separator character.
.TP
\f[B]\-\-type\f[R], \f[B]\-t\f[R] [log|node|observ|sensor|target]
Type of records to import (required).
.TP
\f[B]\-\-verbose\f[R], \f[B]\-V\f[R]
Print progress to \f[I]stdout\f[R].
.TP
\f[B]\-\-version\f[R], \f[B]\-v\f[R]
Print version information and quit.
.SH EXIT STATUS
.TP
\f[B]0\f[R]
Success.
Records have been imported.
.TP
\f[B]1\f[R]
Failure.
Import of records failed.
.SH EXAMPLE
Import observations from CSV file into database:
.IP
.EX
$ dmimport \-t observ \-i observ.csv \-d observ.db \-V
.EE
.SH SEE ALSO
\f[I]dmexport(1)\f[R]
